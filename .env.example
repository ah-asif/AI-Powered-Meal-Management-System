# Copy to `.env` in the repo root before running `docker compose up`.
# These values are consumed by docker-compose.yml for all three services.

DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=student_meal_app
DB_PORT=5432

JWT_SECRET=change-this-to-a-long-random-string

BACKEND_PORT=4000
FRONTEND_PORT=8080