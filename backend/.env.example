PORT=4000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=student_meal_app
DB_POOL_MAX=10

# Auth
JWT_SECRET=change-this-to-a-long-random-string

# Set to true to skip loading seed.sql when running com.mealapp.tools.InitDb
SKIP_SEED=false
